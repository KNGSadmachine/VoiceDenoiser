# VoiceDenoiser バイノーラル自動編集ロードマップ

更新日: 2026-07-26

## 1. 目標

VoiceDenoiser を、単なるノイズ除去ツールから、バイノーラル音声の定位・距離感・左右差を守りながら二段階で自動編集するツールへ移行する。

対象ワークフローは次のとおり。

```text
素材
  ↓
Phase 1: De-plosive → Mouth De-click
  ↓
個別修復（人手）
  ↓
Phase 2: ノイズリダクション → EQ → DeEsser
  ↓
手動の音量調整
  ↓
ラウドネス一致
```

初期リリースで自動化するのは Phase 1 と Phase 2 のみとする。個別修復と音量調整は、意図や演技を人間が判断すべき工程として自動化しない。ラウドネス一致は Phase 1 / Phase 2 には混ぜず、両フェーズが安定した後に独立した仕上げ機能として追加する。

## 2. 最優先の品質原則

1. **バイノーラル情報を守る**
   - L/R をモノラル化しない。
   - L/R 間でサンプル位置をずらさない。
   - チャンネル間の時間差（ITD）、音量差（ILD）、位相関係を、修復対象でない区間では変えない。
   - 片耳側だけにある声、息、耳元動作を「左右差＝異常」と判定しない。

2. **必要な箇所だけを処理する**
   - 常時強いフィルターを掛けず、イベント検出と信頼度に基づく局所処理を基本にする。
   - 判定の信頼度が低い箇所は強く処理せず、要確認箇所としてレポートする。
   - ASMR に必要な息、湿度感、近接効果、舌音まで一律に除去しない。

3. **人手工程を安全に挟める**
   - Phase 1 と Phase 2 は独立して実行できる。
   - Phase 1 後のファイルを外部エディターで修復しても、Phase 2 から再開できる。
   - 元素材は上書きしない。

4. **音量を勝手に仕上げない**
   - Phase 1 / Phase 2 ではノーマライズ、無音カット、ラウドネス合わせを実行しない。
   - 処理によるピーク値・ラウドネス変化は測定してレポートする。
   - 最終音量を決める前にリミッターで波形を変形させない。

## 3. 推奨する製品仕様

### 3.1 二つの実行モード

メイン画面には次の二つを明確に分けて表示する。

#### Phase 1 — クリック・破裂音修復

処理順:

1. De-plosive
2. Mouth De-click

主な入力は録音直後のバイノーラル素材。出力は個別修復に渡す中間マスターとする。

#### Phase 2 — 音質調整

処理順:

1. ノイズリダクション
2. EQ
3. DeEsser

主な入力は個別修復済み素材。Phase 1 の出力に限らず、外部エディターで修復・書き出ししたファイルも受け付ける。

### 3.2 デフォルト設定

人間の介入を減らすため、通常は「自動・保守的」プリセットだけで実行できるようにする。強度の詳細設定は「詳細設定」に隠す。

- バイノーラル 2ch を必須とし、mono や 3ch 以上は自動変換せず明確なエラーにする。
- WAV / FLAC をマスター形式として推奨する。
- MP3 / OGG は読み込み可能だが、再圧縮を避けるため出力を WAV にする。
- サンプルレート、チャンネル数、サンプル数を維持する。
- 中間出力は既定で 32-bit float WAV とし、複数工程での量子化劣化を避ける。
- 出力済みという理由だけではスキップせず、入力指紋・設定・処理バージョンが一致した場合だけ再利用する。
- 元ファイルと同じ場所には書き込まず、上書きも行わない。

推奨ディレクトリ:

```text
dataset/
  raw/          # 録音素材
  phase1/       # Phase 1 自動処理後
  repaired/     # 個別修復済み
  phase2/       # Phase 2 自動処理後
  reports/      # 検出イベント・測定値・警告
```

フォルダを強制はしない。ドラッグ＆ドロップと任意フォルダ指定も引き続き利用できるようにする。

## 4. バイノーラル向け処理設計

### 4.1 共通ステレオ基盤

各エフェクトを実装する前に、次の共通基盤を作る。

- L/R を完全に同期したフレーム境界で解析する。
- 検出値は L/R 個別とステレオ全体の両方を保持する。
- イベントの開始・終了時刻は両チャンネルで共有し、補正量だけを必要に応じてチャンネル別にする。
- 左右で異なる補正を行う場合は差の上限を設け、定位が急に飛ばないよう時間方向に平滑化する。
- フィルター係数、クロスオーバー、処理遅延は L/R で一致させる。
- クロスフェードを付け、イベント境界で新しいクリックを発生させない。
- 処理前後のサンプル数が変わる操作、時間軸上の切り詰め、左右間のクロスフィードは行わない。

### 4.2 Phase 1: De-plosive

検出:

- 短時間の低域エネルギー上昇、低域と中域の比、立ち上がり速度を組み合わせる。
- L/R 個別のスコアを持ち、片耳側だけに強く入った破裂音も検出する。
- 通常の近接効果や低い声が継続している区間は、破裂音と区別する。

修復:

- 検出区間だけに動的ローカットまたは低域シェルフを適用する。
- まずカットのみで補正し、失われた低音を生成しない。
- 高信頼度のイベントは自動修復する。
- 中信頼度は弱く補正し、レポートに残す。
- 低信頼度は音を変更せず、要確認として記録する。

### 4.3 Phase 1: Mouth De-click

検出:

- 数サンプルから数十 ms の短いインパルスを、振幅微分、局所中央値からの外れ、スペクトル変化で検出する。
- 息、子音、囁きの高域、意図的な口音を誤って消さないよう、継続時間と前後文脈を評価する。
- L/R の同時イベントと片側イベントを区別する。

修復:

- クリック区間を削除して時間を詰めず、局所補間または短時間スペクトル修復を使う。
- 片側だけのクリックは原則としてそのチャンネルだけを修復する。
- 両側に相関したクリックは同じ時間境界で修復する。
- 修復範囲とクロスフェードを最小限にする。

### 4.4 Phase 2: ノイズリダクション

現在の「L/R を別々に AI モデルへ渡す」方式は、左右で異なる抑圧が起き得るため、バイノーラル用の既定方式にはしない。

推奨方式:

- 音声区間とノイズ区間をステレオ全体から推定する。
- L/R の複素 STFT に同一の時間周波数ゲインマスクを適用し、元のチャンネル間位相差と音量比を維持する。
- どちらか一方に有効な声がある時間周波数ビンでは、過剰抑圧しない側を優先する。
- 最大減衰量を保守的に制限し、残留ノイズの完全除去より声質と空間情報を優先する。
- DeepFilterNet はノイズ推定候補として評価するが、モデル出力をそのまま左右別々に採用せず、共通マスク生成または同一 dry/wet 制御を通す。
- Resemble Enhance の音声再生成を伴うモードは、定位・声質の検証を通るまで既定処理から外す。

### 4.5 Phase 2: EQ

- L/R に同一のフィルターを適用する。
- 録音全体の傾向から、ランブル、過剰なこもり、強い共鳴だけを補正する。
- 演者や距離ごとの音色を同じ目標カーブへ強制しない。
- 初期版はカット中心の corrective EQ とし、自動ブーストは行わない。
- 各バンドの補正量と Q に上限を設ける。
- 補正の根拠、周波数、ゲインをレポートに残す。
- 補正不要と判断した場合は EQ を自動でバイパスする。

### 4.6 Phase 2: DeEsser

- 4 kHz から 12 kHz を固定で削るのではなく、録音ごとに歯擦音帯域を推定する。
- L/R 個別の検出値と共有タイムラインを使う。
- 片耳側に定位した発話を考慮し、補正量はチャンネル別にできるようにする。
- 左右の補正差には上限を設け、歯擦音の瞬間だけ定位が飛ぶことを防ぐ。
- split-band 方式を既定とし、声全体の音量を下げない。
- 最大減衰量を制限し、舌足らずな音になる前に止める。

## 5. 成果物と再開設計

各出力音声と同じベース名で、処理記録の JSON を保存する。

記録する内容:

- 元ファイルの相対パス、サイズ、更新時刻、内容ハッシュ
- 入出力のフォーマット、サンプルレート、チャンネル数、サンプル数
- 実行した Phase、アプリと処理アルゴリズムのバージョン
- 使用プリセットと実際に適用されたパラメーター
- 検出イベントの時刻、対象チャンネル、信頼度、補正量
- 処理前後の peak、true peak、RMS、LUFS-I の測定値
- ITD / ILD / 位相相関の変化量
- スキップ、要確認、クリップなどの警告

書き出しは一時ファイルに完了してから最終名へ移動する。途中停止で壊れたファイルを完成品として扱わない。

Phase 1 後には、人間が個別修復しやすいよう次を出力する。

- 修復済み音声
- De-plosive / Mouth De-click のイベント一覧
- 自動処理しなかった低信頼度イベントの要確認一覧
- 試聴開始位置へ移動できるタイムコード

Phase 2 は sidecar JSON がなくても実行可能にする。外部エディター経由で履歴が切れている場合だけ「来歴不明」とレポートし、処理自体は止めない。

## 6. UI 方針

### メイン画面

- 最上部に「Phase 1」「Phase 2」の大きなモード選択を置く。
- 選択中の Phase に含まれる処理順を常に表示する。
- 入力、出力、試聴、一括実行、進捗、結果を Phase ごとに独立させる。
- 既定プリセットは「自動・保守的」とする。
- 詳細設定を開かなくても実行できるようにする。
- 実行前にチャンネル数、サンプルレート、出力衝突、空き容量を検査する。

### 試聴

- 処理前後を同じ再生位置・同じ表示ゲインで比較する。
- 原音と処理後のラウドネス差で「良くなった」と錯覚しないよう、試聴時だけモニター音量を一致させる。ファイル自体には適用しない。
- 全体だけでなく、検出イベント周辺を順番に試聴できるようにする。
- L、R、Stereo、Mid、Side のモニター切り替えを後続リリースで追加する。

### 既存機能の扱い

- 「通常モード」と「バイノーラル保持モード」の選択は廃止し、Phase 1 / Phase 2 は常にバイノーラル安全モードで処理する。
- 現在のエンジン選択はメイン画面から外し、比較検証用の詳細設定へ移す。
- ノーマライズと無音カットは Phase 1 / Phase 2 から外す。
- 既存の汎用 mono 処理は移行期間だけ Legacy として分離し、バイノーラル処理と混在させない。

## 7. 実装マイルストーン

### M0 — 仕様固定と評価素材

目的: 「音がきれい」だけでなく、バイノーラルを壊していないか判定できる基準を作る。

- [ ] 2ch 専用の入出力契約をテストで固定
- [ ] 48 kHz / 96 kHz、16-bit / 24-bit / float の評価パターンを用意
- [ ] 左、右、中央、移動、耳元、囁き、息、無音を含む評価クリップを用意
- [ ] plosive、mouth click、定常ノイズ、歯擦音の正解タイムコードを作成
- [ ] 現行 DeepFilterNet / Resemble Enhance の処理前後を基準測定
- [ ] 自動評価指標と人間による試聴シートを確定

完了条件:

- 同じ素材・設定で比較結果を再現できる。
- 現行方式が ITD / ILD / 位相へ与える影響を数値で確認できる。

### M1 — 安全な音声 I/O と処理基盤

目的: どのエフェクトを追加しても左右同期と再開安全性が崩れない土台を作る。

- [ ] UI と処理ロジックを `app.py` から分離
- [ ] Phase、ステップ、プリセットを表す設定モデルを追加
- [ ] 2ch 検証、PCM subtype 取得、float 内部処理を実装
- [ ] サンプルレート・チャンネル数・サンプル数の保持を実装
- [ ] sidecar JSON、入力指紋、処理バージョンを実装
- [ ] 原子的書き出しと安全なレジュームを実装
- [ ] ステレオ共通フレーム、イベント、ゲイン包絡の基盤を実装
- [ ] 旧 `denoise_file` をステップ実行型パイプラインへ移行

完了条件:

- 無処理パスで音声データ、長さ、サンプルレート、チャンネル順が保持される。
- 同名出力があっても、異なる入力や設定を誤ってスキップしない。
- 中断後に不完全な出力が残らない。

### M2 — Phase 1

目的: De-plosive と Mouth De-click を局所的かつ検証可能に自動化する。

- [ ] De-plosive の検出器と信頼度を実装
- [ ] 動的ローカット / 低域シェルフ修復を実装
- [ ] Mouth De-click の検出器と信頼度を実装
- [ ] 局所補間 / スペクトル修復を実装
- [ ] 左右イベント統合とチャンネル別補正制限を実装
- [ ] イベントレポートとタイムコード出力を実装
- [ ] Phase 1 の試聴と一括処理 UI を実装
- [ ] 「自動・保守的」プリセットを評価素材で調整

完了条件:

- Phase 1 の順序が常に De-plosive → Mouth De-click になる。
- 既知イベントの検出率と誤検出率が M0 の目標値を満たす。
- 修復対象外の区間で定位が変化しない。
- 個別修復へ渡すファイルと要確認一覧を一操作で生成できる。

### M3 — Phase 2

目的: ノイズ、不要な共鳴、歯擦音を抑えつつ、バイノーラル空間を維持する。

- [ ] ステレオリンク型ノイズ推定・共通ゲインマスクを実装
- [ ] DeepFilterNet を共通マスク生成候補として比較
- [ ] 最大減衰量と音声保護ロジックを実装
- [ ] ステレオ共通 corrective EQ の自動分析を実装
- [ ] EQ のバイパス判定と補正上限を実装
- [ ] ステレオ連携 DeEsser を実装
- [ ] Phase 2 の試聴と一括処理 UI を実装
- [ ] 処理前後の品質・空間・音量レポートを実装

完了条件:

- Phase 2 の順序が常にノイズリダクション → EQ → DeEsser になる。
- 修復対象外の L/R 間サンプル遅延が変化しない。
- 定位変化、ミュージカルノイズ、声質変化が M0 の基準内に収まる。
- EQ が不要な素材では自動的にバイパスされる。

### M4 — 人手工程との受け渡し

目的: Phase 間の手作業を迷わず、少ない操作で行えるようにする。

- [ ] `raw → phase1 → repaired → phase2` の推奨フォルダを UI から作成
- [ ] Phase 1 の要確認イベントをファイル単位・時刻順に表示
- [ ] 外部修復済みファイルと Phase 1 元ファイルの対応付けを実装
- [ ] Phase 2 入力の来歴・二重処理警告を実装
- [ ] 失敗ファイルだけを再実行できるようにする
- [ ] バッチ終了サマリーを JSON / CSV で出力

完了条件:

- アプリを閉じても作業状態を復元できる。
- 個別修復済みファイルだけを Phase 2 へ安全に送れる。
- 同じ Phase の意図しない二重処理を防げる。

### M5 — 実録音による品質調整とベータ版

目的: 合成テストだけでなく、実際の長尺バイノーラル作品で安全性を確認する。

- [ ] 複数マイク、演者、距離、部屋、ノイズ条件で長尺テスト
- [ ] 片耳囁き、左右移動、耳元の息、意図的な口音を重点確認
- [ ] CPU / CUDA、Windows / macOS で結果と性能を確認
- [ ] 途中停止、ディスク不足、壊れた音声、モデル取得失敗を確認
- [ ] 既定プリセットを実録音の試聴結果から調整
- [ ] README と画面キャプチャを新ワークフローへ更新
- [ ] Legacy 機能の廃止可否を判断

完了条件:

- 代表的な長尺素材を無人で最後まで処理できる。
- 要確認イベントだけを人間が追えばよい状態になる。
- バイノーラル品質の重大な劣化がないことを試聴者が確認する。

### M6 — ラウドネス仕上げ（Phase 1 / Phase 2 安定後）

目的: 手動音量調整後の素材を、別工程として指定ラウドネスへ一致させる。

- [ ] LUFS-I、LRA、true peak の測定だけを先に提供
- [ ] 目標値と納品仕様をプロジェクト単位で保存
- [ ] ゲイン変更だけで達成できる場合と、リミッターが必要な場合を区別
- [ ] リミッターが必要な場合は自動実行せず警告する初期仕様を検討
- [ ] Phase 2 の出力へ自動適用せず、明示的な別実行にする

このマイルストーンは Phase 1 / Phase 2 の完成条件には含めない。

## 8. 自動テストと受け入れ基準

### 必須の不変条件

- 入出力が常に 2ch のまま。
- サンプルレートが変わらない。
- サンプル数が変わらない。
- L/R の並びが入れ替わらない。
- 処理による L/R 間の新規遅延が 0 sample。
- NaN、Inf、範囲外サンプル、意図しないクリップがない。
- 同じ入力、設定、処理バージョンなら結果が再現可能。

### 空間品質

- 修復対象外区間の ILD 変化を原則 0.25 dB 以内にする。
- 修復対象外区間のチャンネル間位相差を実質的に維持する。
- 左右に移動する音源の軌跡が処理前後で連続している。
- 片側だけの発話や息が中央へ寄らない。

数値閾値は M0 の実素材測定で確定する。閾値を緩める場合は、アルゴリズムの都合ではなく試聴上の妥当性を記録する。

### 音質

- De-plosive 後に声の胴鳴りが継続的に失われない。
- Mouth De-click 後に新しいクリックや無音の穴が生じない。
- ノイズリダクション後にミュージカルノイズが目立たない。
- EQ 後に距離感と近接効果が不自然に均一化されない。
- DeEsser 後に子音の明瞭度が失われない。

### バッチ動作

- 0 byte、読めないファイル、非 2ch ファイルを個別に失敗扱いとし、バッチ全体は継続する。
- 失敗理由をファイルごとに残す。
- 中断後は一致する成果物だけをスキップして再開する。
- 出力先が入力配下でも、自分の出力を再帰的に再処理しない。

## 9. 主なリスクと対策

| リスク | 対策 |
|---|---|
| mouth click と意図的な口音を区別できない | 保守的な既定値、信頼度別処理、低信頼度はレポートのみ |
| 片耳発話をノイズと誤判定する | L/R のどちらかに音声があれば保護する共通マスク |
| 左右別 AI 処理で定位が揺れる | モデル出力を直接採用せず、ステレオリンクした補正へ変換 |
| EQ で作品ごとの距離感が均一化する | カット中心、補正上限、不要時バイパス |
| 長尺ファイルでメモリ不足になる | チャンク処理、オーバーラップ、状態引き継ぎ、原子的書き出し |
| 複数回の WAV 書き出しで劣化する | 内部 float、32-bit float 中間マスター、非可逆再圧縮を避ける |
| 「処理済み」だけで古い出力を再利用する | 入力指紋・設定・バージョン一致によるレジューム |
| 自動化で判断箇所が見えなくなる | イベント、信頼度、補正量、測定値を sidecar と画面へ表示 |

## 10. 初期スコープ外

- 個別修復そのものの完全自動化
- 手動音量オートメーションの自動生成
- Phase 2 と同時のラウドネス正規化
- モノラル素材の自動ステレオ化
- 3ch 以上や Ambisonics の処理
- 左右の位置補正、クロストーク除去、ルーム補正
- 音声の生成・声質変換・失われた演技の生成

## 11. 推奨する着手順

`M0 → M1 → M2 → M3 → M4 → M5` の順で進める。

特に M1 のステレオ不変条件と M0 の評価基準を飛ばして DSP を増やさない。Phase 1 を先に完成させ、個別修復へ安全に渡せる状態を作ってから Phase 2 に進む。ラウドネス仕上げは二つの Phase と責務を分離し、M6 として最後に扱う。
